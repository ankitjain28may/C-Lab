#include <stdio.h>
main()
{ 
printf("Enter the radius for calculating the radius of circle");
float r,c;
scanf("%f",&r);
c=2*3.14*r;
printf("Circumference of the circle with radius %f is %f:",r,c);
}
